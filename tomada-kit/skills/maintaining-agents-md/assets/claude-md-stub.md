<!-- agents-md-sync:begin -->
@AGENTS.md
<!-- agents-md-sync:end -->

# Claude Code specifics

<!--
The three lines above the blank line are the managed block, byte for byte:
`scripts/sync_stubs.py` owns them and rewrites them whenever they drift. It
embeds those bytes as a constant, so this file is documentation and a
copy-paste source, never an input to the script.

Everything below the end marker is the free section. It is hand-maintained and
preserved byte for byte by every sync. Keep it to mechanics that exist only on
this host, for example:

- hooks and settings in `.claude/settings.json`
- `.claude/rules/*.md` that are deliberately staying host-only, listed here so
  a reader knows they exist
- skills or commands this repository ships for Claude Code alone

Anything tool-agnostic — commands, architecture, conventions, gotchas — belongs
in AGENTS.md, which both hosts read. Delete this whole section, heading
included, when there is nothing host-specific to say; a bare managed block is
the normal shape.
-->
